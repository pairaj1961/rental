{
  "buildCommand": "pnpm --filter equipment-rental build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
